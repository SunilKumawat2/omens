module.exports = {
  mode: 'jit', // Enable JIT mode (if not enabled by default)
  content: [
    "./src/**/*.{js,jsx,ts,tsx}", // Make sure the paths are correct
  ],
  theme: {
    extend: {},
  },
  plugins: [],
};
